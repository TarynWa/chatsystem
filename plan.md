## 🎯 分布式IM系统 - 完整架构方案

基于我们之前的所有讨论，我为您整理这套C++自研分布式IM系统的**完整架构方案**。这是一个可以直接写入简历、冲击大厂offer的完整项目设计。

---

## 一、项目概述

### 1.1 项目定位

```
项目名称: 企业级分布式即时通讯系统
技术路线: C++自研全栈 + 微服务架构
设计目标:
  - 日活用户: 10万+
  - 同时在线: 5万+
  - 消息吞吐: 10000 TPS
  - 系统可用性: 99.99%
  - 消息延迟: P99 < 100ms

核心价值:
  ✅ 完全自主可控 (自研全部基础组件)
  ✅ 极致性能优化 (C++ + 零拷贝 + 无锁)
  ✅ 生产级高可用 (熔断/降级/限流/容灾)
  ✅ 完整功能覆盖 (IM全场景)
```

### 1.2 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **编程语言** | C++17/20 | 高性能、内存控制 |
| **网络库** | 自研 Reactor+Epoll | 百万并发、边缘触发 |
| **RPC框架** | 自研 TCP+Protobuf | 服务治理、负载均衡 |
| **消息队列** | 自研 共享内存+磁盘 | 零拷贝、持久化 |
| **线程池** | 自研 工作窃取 | 负载均衡、CPU亲和 |
| **日志库** | 自研 异步双缓冲 | 非阻塞、零拷贝 |
| **数据库** | MySQL 8.0 | 主从、分库分表 |
| **缓存** | Redis 7.0 | 哨兵、多级缓存 |
| **检索** | Elasticsearch 8.x | 全文检索、冷数据 |
| **服务发现** | Consul | 注册、健康检查 |
| **负载均衡** | LVS + Nginx | 四层+七层 |
| **容器编排** | Kubernetes | 自动扩缩容 |
| **监控** | Prometheus + Grafana | 全链路监控 |

---

## 二、系统架构图

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         客户端层                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │   iOS    │ │ Android  │ │   Web    │ │   PC    │        │
│  │ (Swift)  │ │  (Java)  │ │(JS/Wasm) │ │(Qt/C++) │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
│                    WebSocket长连接 + Protobuf                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         接入层                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  LVS (四层负载均衡) + Nginx (七层负载均衡 + 反向代理)   │  │
│  │  • 健康检查 • SSL终止 • 限流 • 静态资源缓存             │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         网关层                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  API Gateway (自研/集成)                                │  │
│  │  • 统一鉴权 • 动态路由 • 流量控制 • 熔断降级           │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         业务层 (C++微服务)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ 认证服务     │  │ 消息服务     │  │ 社交服务     │          │
│  │ AuthService  │  │ MsgService  │  │SocialService│          │
│  │ (3节点)     │  │ (5节点)     │  │ (2节点)     │          │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤          │
│  │ • 登录/注册  │  │ • 单聊      │  │ • 好友管理   │          │
│  │ • 注销      │  │ • 群聊      │  │ • 群组管理   │          │
│  │ • Token管理  │  │ • 离线消息   │  │ • 黑名单    │          │
│  │ • Session管理│  │ • 历史消息   │  │ • 好友推荐   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│                    ↑ RPC同步 ↑    ↑ MQ异步 ↑                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     中间件层 (自研)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  自研RPC     │  │  自研MQ      │  │  自研网络库  │          │
│  │  TCP+Protobuf│  │  共享内存+磁盘│  │  Reactor+Epoll│         │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │  自研线程池  │  │  自研异步日志 │  │  自研内存池  │          │
│  │  工作窃取    │  │  双缓冲      │  │  对象复用    │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         存储层                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  L1缓存: 本地缓存 (Caffeine风格)                        │  │
│  │  L2缓存: Redis (哨兵模式, 3节点)                        │  │
│  │  L3存储: MySQL (一主两从, 分库分表)                     │  │
│  │  L4检索: Elasticsearch (3节点集群)                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      监控运维层                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Prometheus + Grafana (指标监控)                        │  │
│  │  Jaeger (链路追踪)                                     │  │
│  │  ELK (日志分析)                                        │  │
│  │  AlertManager (告警)                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 服务间通信架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      服务通信模型                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌──────────────┐         RPC同步          ┌──────────────┐  │
│   │              │─────────────────────────▶│              │  │
│   │  认证服务    │                          │  消息服务    │  │
│   │              │◀─────────────────────────│              │  │
│   └──────────────┘                          └──────────────┘  │
│          │                                           │        │
│          │ RPC同步                                   │ MQ异步  │
│          ▼                                           ▼        │
│   ┌──────────────┐                          ┌──────────────┐  │
│   │              │                          │              │  │
│   │  社交服务    │                          │  消息队列    │  │
│   │              │                          │  (自研)     │  │
│   └──────────────┘                          └──────────────┘  │
│          │                                           │        │
│          │ MQ异步                                   │ 消费    │
│          └───────────────────────────────────────────┘        │
│                                                                 │
│   通信决策:                                                    │
│   • 用户等待结果 → RPC同步 (登录/发消息/加好友)                │
│   • 用户不感知 → MQ异步 (通知/日志/缓存更新)                   │
│   • 强一致性 → RPC同步 (验证/扣减)                             │
│   • 最终一致性 → MQ异步 (状态同步/数据同步)                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、核心组件详细设计

### 3.1 自研网络库

```
设计模式: Reactor + Proactor混合

┌─────────────────────────────────────────────────────────────────┐
│  EventLoop (事件循环)                                         │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  • 每个线程一个EventLoop                                │    │
│  │  • 驱动所有I/O事件 (Epoll ET模式)                      │    │
│  │  • 定时器管理 (TimerQueue)                            │    │
│  │  • 跨线程调用 (runInLoop)                             │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Channel (事件通道)                                           │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  • 封装文件描述符 (fd)                                 │    │
│  │  • 管理读写事件回调                                     │    │
│  │  • 边缘触发ET + 非阻塞IO                              │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  TcpConnection (TCP连接)                                      │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  • 连接状态管理 (连接中/已连接/断开中/已断开)          │    │
│  │  • 输入/输出缓冲区 (Buffer)                            │    │
│  │  • 高水位/低水位控制 (流量控制)                        │    │
│  │  • 应用层心跳 (30s) + TCP KeepAlive                   │    │
│  │  • 消息编解码 (Codec)                                  │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  Buffer (缓冲区)                                              │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  • 动态扩展 (预分配+按需增长)                         │    │
│  │  • 读写指针分离 (避免数据移动)                         │    │
│  │  • 零拷贝读取 (readv/writev)                          │    │
│  │  • 支持多种编解码器                                     │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

**关键特性**:
```
高并发: 单机支持百万级并发连接
高性能: 边缘触发 + 非阻塞IO + 零拷贝
低延迟: 无锁数据结构 + 内存池
可扩展: 支持多种协议 (TCP/WebSocket/HTTP)
```

### 3.2 自研RPC框架

```
┌─────────────────────────────────────────────────────────────────┐
│  RpcServer (服务端)                                           │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  • 服务注册到Consul                                    │    │
│  │  • 请求路由到对应方法                                   │    │
│  │  • 并发控制 (信号量/线程池)                            │    │
│  │  • 限流保护 (令牌桶)                                   │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│  RpcClient (客户端)                                           │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  • 从Consul获取服务地址列表                            │    │
│  │  • 负载均衡 (轮询/加权/一致性哈希)                    │    │
│  │  • 连接池管理 (复用TCP连接)                           │    │
│  │  • 超时控制 (500ms-3s) + 自动重试 (最多3次)          │    │
│  │  • 熔断降级 (错误率>50%触发)                          │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│  协议格式 (TCP + Protobuf)                                    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  ┌──────────┬──────────┬──────────┬──────────┐        │    │
│  │  │  Magic   │ Version  │   Type   │ Sequence │        │    │
│  │  │  4B      │   2B     │   2B     │   4B     │        │    │
│  │  ├──────────┴──────────┴──────────┴──────────┤        │    │
│  │  │  Body Length (4B)                          │        │    │
│  │  ├─────────────────────────────────────────────┤        │    │
│  │  │  Body (Protobuf序列化)                     │        │    │
│  │  │  • ServiceName                            │        │    │
│  │  │  • MethodName                             │        │    │
│  │  │  • Request/Response Data                  │        │    │
│  │  │  • Metadata (超时/重试/熔断配置)          │        │    │
│  │  └─────────────────────────────────────────────┘        │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

**关键特性**:
```
服务治理: 注册发现/负载均衡/健康检查
高性能: Protobuf序列化 + 连接池复用
高可用: 超时重试/熔断降级/故障摘除
可观测: 全链路追踪 + 性能监控
```

### 3.3 自研消息队列

```
┌─────────────────────────────────────────────────────────────────┐
│  存储架构                                                     │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  内存层 (共享内存)                                      │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │  • 环形缓冲区 (RingBuffer)                     │    │    │
│  │  │  • 固定大小消息槽 (预分配)                     │    │    │
│  │  │  • 无锁CAS操作 (多生产者/多消费者)             │    │    │
│  │  │  • 缓存行对齐 (避免伪共享)                     │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              ↓ (消息溢出)                      │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  文件层 (磁盘存储)                                      │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │  • CommitLog: 顺序写入 (Append Only)           │    │    │
│  │  │  • ConsumerQueue: 消费进度队列                 │    │    │
│  │  │  • IndexFile: 消息索引 (MsgID → Offset)        │    │    │
│  │  │  • 零拷贝 (sendfile)                           │    │    │
│  │  └─────────────────────────────────────────────────┘    │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  消息模型                                                     │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐       │
│  │    Topic    │───▶│  Partition  │───▶│   Message   │       │
│  │   (主题)    │    │   (分区)    │    │   (消息)    │       │
│  └─────────────┘    └─────────────┘    └─────────────┘       │
│                                                                 │
│  可靠性保证:                                                   │
│  • 生产者ACK: 同步刷盘/异步确认                                │
│  • 消费者ACK: 手动提交偏移量 (至少一次消费)                    │
│  • 消息重试: 指数退避 (1s, 2s, 4s, 8s)                        │
│  • 死信队列: 处理消费失败的消息                                │
└─────────────────────────────────────────────────────────────────┘
```

**关键特性**:
```
高性能: 共享内存传输 (<1ms延迟)
高吞吐: 顺序写磁盘 + 零拷贝
持久化: 内存+磁盘混合存储
顺序保证: 分区内消息有序
```

### 3.4 自研异步日志库

```
┌─────────────────────────────────────────────────────────────────┐
│  架构设计                                                     │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     │
│  │  业务线程   │────▶│  缓冲区A    │     │  磁盘写入   │     │
│  │  (生产者)   │     │  (当前)     │     │  (异步)     │     │
│  └─────────────┘     └─────────────┘     └─────────────┘     │
│                             │                   ↑              │
│                             ▼                   │              │
│                      ┌─────────────┐     ┌─────────────┐      │
│                      │  缓冲区B    │────▶│  后台线程   │      │
│                      │  (备用)     │     │  (消费者)   │      │
│                      └─────────────┘     └─────────────┘      │
└─────────────────────────────────────────────────────────────────┘

关键特性:
• 双缓冲切换: 缓冲区满(100MB)或定时(3s)切换
• 日志级别: TRACE < DEBUG < INFO < WARN < ERROR < FATAL
• 日志格式: [时间] [线程ID] [级别] [文件:行号] 消息
• 滚动策略: 按大小(100MB) / 按时间(每天) / 按数量(保留10个)
• 性能优化: 内存池分配 + 批量写入 (减少系统调用)
```

---

## 四、业务服务设计

### 4.1 认证服务 (AuthService)

```
功能模块:
┌─────────────────────────────────────────────────────────────────┐
│  认证服务                                                      │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  1. 用户管理                                             │  │
│  │     • 注册: 用户名/密码/手机号/邮箱                      │  │
│  │     • 登录: 密码验证 (bcrypt) + 生成JWT Token           │  │
│  │     • 注销: 清除Session + 通知其他服务                   │  │
│  │     • 信息修改: 昵称/头像/密码/手机号/邮箱               │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  2. 会话管理                                             │  │
│  │     • Token生成/验证/刷新                                │  │
│  │     • 多端登录管理 (最多5台设备)                         │  │
│  │     • 踢人下线 (强制其他设备退出)                        │  │
│  │     • 会话过期处理 (7天无操作自动过期)                   │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  3. 安全控制                                             │  │
│  │     • 密码加密 (bcrypt, cost=10)                        │  │
│  │     • 防暴力破解 (5次失败锁定30分钟)                     │  │
│  │     • 登录审计 (记录IP/设备/时间)                        │  │
│  │     • 操作日志 (所有敏感操作记录)                        │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

关键流程:
登录流程:
  Client → 验证密码 → 生成Token → 查询未读数(RPC) → 异步通知(MQ) → 返回

会话同步:
  Session存储于Redis (7天TTL) + 本地缓存 (5分钟)
  多端登录: 每个设备独立Session
```

### 4.2 消息服务 (MessageService)

```
功能模块:
┌─────────────────────────────────────────────────────────────────┐
│  消息服务                                                      │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  1. 单聊消息                                             │  │
│  │     • 发送: 存储 + 实时推送 (在线) / 离线存储 (离线)    │  │
│  │     • 接收: 在线推送 / 离线拉取                         │  │
│  │     • 已读回执: 更新状态 + 通知发送方                    │  │
│  │     • 撤回消息: 2分钟内可撤回                            │  │
│  │     • 删除消息: 软删除 (仅删除本地)                      │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  2. 群聊消息                                             │  │
│  │     • 发送: 写入群消息表 + 推送群成员                    │  │
│  │     • 推送策略: 小群推模式 / 大群拉模式                  │  │
│  │     • @功能: 通知指定用户                                │  │
│  │     • 群公告: 置顶消息                                   │  │
│  │     • 群禁言: 管理员禁言指定成员                         │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  3. 离线消息                                             │  │
│  │     • 存储: 用户离线期间消息存到offline_messages        │  │
│  │     • 拉取: 用户上线时批量拉取 (增量)                    │  │
│  │     • 清理: 拉取后删除或标记已读                        │  │
│  │     • 过期清理: 7天自动删除                              │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  4. 历史消息                                             │  │
│  │     • 存储: MySQL (热数据7天) + ES (冷数据)             │  │
│  │     • 查询: 按时间/关键词/用户分页拉取                   │  │
│  │     • 导出: 导出聊天记录 (管理员)                        │  │
│  │     • 清理: 定期归档到ES                                │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  5. 消息推送                                             │  │
│  │     • 在线推送: WebSocket直接推送 (RPC同步)             │  │
│  │     • 离线推送: APNS/FCM/个推 (MQ异步)                 │  │
│  │     • 多端同步: 所有设备同时收到消息                    │  │
│  │     • 推送ACK: 确认收到 + 重试机制                      │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

关键流程:
发送消息:
  验证权限 → 生成MsgID → 存储消息 → 查询在线状态
    ├─ 在线 → 实时推送 → 等待ACK
    └─ 离线 → 存储离线消息
  异步任务: 更新未读数 → 写入ES索引 → 更新缓存

消息可靠性:
  • 客户端: 本地持久化 + 重试队列
  • 服务端: ACK确认 + 去重机制 (MsgID)
  • 端到端: 状态机 (发送中→已送达→已读)
```

### 4.3 社交服务 (SocialService)

```
功能模块:
┌─────────────────────────────────────────────────────────────────┐
│  社交服务                                                      │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  1. 好友管理                                             │  │
│  │     • 添加好友: 发送申请 → 对方确认 → 建立关系          │  │
│  │     • 删除好友: 双方解除关系 + 清理消息                  │  │
│  │     • 好友列表: 按时间/字母排序                         │  │
│  │     • 好友搜索: 按昵称/手机号/邮箱搜索                  │  │
│  │     • 好友备注: 修改备注名                              │  │
│  │     • 好友推荐: 共同好友/同群组推荐                     │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  2. 群组管理                                             │  │
│  │     • 创建群组: 设置名称/头像/公告                       │  │
│  │     • 加入群组: 申请/邀请/直接加入                       │  │
│  │     • 退出群组: 主动退出/被踢出                         │  │
│  │     • 解散群组: 仅群主可解散                           │  │
│  │     • 群设置: 群名称/头像/公告/禁言/验证               │  │
│  │     • 群管理员: 设置/撤销管理员                         │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  3. 黑名单管理                                           │  │
│  │     • 加入黑名单: 屏蔽对方消息                           │  │
│  │     • 移除黑名单: 解除屏蔽                               │  │
│  │     • 黑名单列表: 查看所有屏蔽用户                       │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

关键流程:
加好友:
  发送申请 → 存入friend_requests → 推送通知 (MQ)
  对方确认 → 写入friends表 (双向) → 更新缓存 (MQ)
  异步任务: 更新好友列表缓存 → 推送好友上线通知

群组管理:
  创建群组 → 写入groups表 → 写入group_members (群主)
  邀请成员 → 写入group_members (待确认) → 推送邀请通知
  成员确认 → 更新状态 → 推送入群通知
```

---

## 五、数据流设计

### 5.1 核心数据流

```
登录数据流:
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ Client  │───▶│ Nginx   │───▶│ Auth    │───▶│ MySQL   │
│         │    │         │    │ Server  │    │ (验证)  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
                                    │
                                    ├──▶ Redis (存Session)
                                    │
                                    ├──▶ RPC → MsgServer (查未读数)
                                    │
                                    └──▶ MQ → 通知好友上线
                                    │
                                    ▼
                              ┌─────────┐
                              │ Client  │
                              │ (返回)  │
                              └─────────┘

发送消息数据流:
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│ Client  │───▶│ Nginx   │───▶│ Auth    │───▶│ Msg     │
│         │    │         │    │ Server  │    │ Server  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘
                                    │               │
                                    │              ├──▶ MySQL (存消息)
                                    │              │
                                    │              ├──▶ Redis (查在线)
                                    │              │
                                    │              ├──▶ WebSocket (推送)
                                    │              │
                                    │              └──▶ MQ (异步任务)
                                    │                      │
                                    │                      ├──▶ 更新未读数
                                    │                      │
                                    │                      └──▶ 写入ES
                                    ▼
                              ┌─────────┐
                              │ Client  │
                              │ (返回)  │
                              └─────────┘
```

### 5.2 异步任务流 (MQ)

```
主题: user.online
  生产者: AuthServer (用户登录时)
  消费者: 
    - MsgServer: 更新在线状态 (Redis)
    - SocialServer: 通知好友上线 (推送)
    - LogServer: 记录登录日志

主题: msg.sent
  生产者: MsgServer (消息发送时)
  消费者:
    - MsgServer: 更新未读计数 (Redis)
    - ESServer: 建立ES索引
    - CacheServer: 更新缓存

主题: friend.update
  生产者: SocialServer (好友变更时)
  消费者:
    - CacheServer: 更新好友列表缓存
    - MsgServer: 刷新好友在线状态
    - PushServer: 推送好友状态变更

主题: audit.log
  生产者: 所有服务 (敏感操作时)
  消费者:
    - LogServer: 写入审计日志表
    - AnalyticsServer: 行为分析

主题: cache.invalidate
  生产者: 所有服务 (数据变更时)
  消费者:
    - CacheServer: 删除对应缓存
    - 所有服务: 刷新本地缓存
```

---

## 六、部署架构

### 6.1 Kubernetes部署

```yaml
命名空间: im-system

服务部署:
  # 接入层
  nginx-ingress:
    replicas: 2
    resources: 2C/4G
  
  # 业务层
  auth-service:
    replicas: 3
    resources: 4C/8G
    HPA: CPU > 70% → 扩容
    
  msg-service:
    replicas: 5
    resources: 8C/16G
    HPA: CPU > 70% → 扩容
    
  social-service:
    replicas: 2
    resources: 2C/4G
    HPA: CPU > 70% → 扩容
  
  # 中间件层
  consul:
    replicas: 3
    resources: 1C/2G
    
  mq-service:
    replicas: 3
    resources: 4C/8G
    
  # 存储层
  mysql:
    replicas: 3 (1主2从)
    resources: 8C/32G
    
  redis:
    replicas: 3 (1主2从)
    resources: 2C/8G
    
  elasticsearch:
    replicas: 3
    resources: 8C/32G

自动扩缩容:
  auth-service: min=3, max=10, targetCPU=70%
  msg-service: min=5, max=15, targetCPU=70%
  social-service: min=2, max=8, targetCPU=70%
```

### 6.2 多机房部署

```
主机房 (华北):
  - 全量部署 (所有服务)
  - 处理95%流量
  - 主数据库 (读写)

备机房 (华东):
  - 核心服务部署 (Auth/Msg)
  - 处理5%流量 (就近接入)
  - 从数据库 (只读)

灾备策略:
  - 数据同步: 异步复制 (延迟<1s)
  - 故障切换: DNS解析切换 (RTO<5min)
  - 数据丢失: < 30s (异步复制延迟)
```

---

## 七、性能优化策略

### 7.1 优化目标

| 指标 | 目标值 | 优化手段 |
|------|--------|----------|
| 登录QPS | 6500 | Nginx集群 + 连接池 + 缓存 |
| 消息延迟(P99) | 85ms | 推拉结合 + 批量处理 |
| 好友列表加载 | 45ms | 多级缓存 + 覆盖索引 |
| 离线消息拉取 | 150ms | 增量拉取 + 分页优化 |
| 缓存命中率 | 95% | 多级缓存 + 预热策略 |
| 系统可用性 | 99.98% | 熔断降级 + 集群容灾 |

### 7.2 优化手段

```
网络层:
  ✅ 长连接复用 (WebSocket持久化)
  ✅ 连接池 (RPC连接复用)
  ✅ 零拷贝 (sendfile/splice)
  ✅ Protobuf序列化 (比JSON快3-5倍)
  ✅ 消息压缩 (gzip, 压缩率60-80%)

内存层:
  ✅ 内存池 (减少分配开销)
  ✅ 对象池 (复用对象)
  ✅ 缓存行对齐 (避免伪共享)
  ✅ 智能指针 (自动内存管理)

CPU层:
  ✅ 无锁数据结构 (CAS)
  ✅ 工作窃取线程池 (负载均衡)
  ✅ CPU亲和性 (绑定核心)
  ✅ 批量处理 (减少调度开销)

存储层:
  ✅ 分库分表 (1024张表)
  ✅ 读写分离 (1主2从)
  ✅ 覆盖索引 (减少回表)
  ✅ 异步写入 (批量刷盘)
```

---

## 八、监控与告警

### 8.1 监控体系

```
系统监控:
  • CPU使用率/负载
  • 内存使用率
  • 磁盘IO/空间
  • 网络流量/连接数

应用监控:
  • 服务状态 (运行/停止/降级)
  • QPS/TPS (每秒请求数)
  • 响应时间 (P50/P95/P99)
  • 错误率 (4xx/5xx比例)
  • 连接数 (活跃/空闲)

业务监控:
  • 在线用户数
  • 消息发送量 (每分钟)
  • 消息延迟 (发送→送达)
  • 离线消息积压量
  • 好友添加/删除量

中间件监控:
  • MySQL: 连接数/QPS/慢查询/主从延迟
  • Redis: 连接数/命中率/内存使用
  • ES: 索引大小/查询延迟
  • MQ: 消息积压/消费速率
```

### 8.2 告警规则

```yaml
系统告警:
  - CPU > 80% → 告警 (建议扩容)
  - 内存 > 85% → 告警 (建议扩容)
  - 磁盘 > 80% → 告警 (清理数据)
  - 连接数 > 80% → 告警 (增加节点)

应用告警:
  - 错误率 > 5% → 告警 (需排查)
  - 响应时间 P99 > 500ms → 告警 (性能下降)
  - QPS突降 > 50% → 告警 (服务可能异常)
  - 服务不可用 → 立即告警 (P0级别)

业务告警:
  - 在线用户数 < 50% → 告警 (可能故障)
  - 离线消息积压 > 10000 → 告警 (消费能力不足)
  - 消息发送量突降 > 50% → 告警 (可能异常)

中间件告警:
  - MySQL主从延迟 > 5s → 告警
  - 慢查询 > 10/min → 告警 (需优化)
  - Redis内存 > 90% → 告警 (需扩容)
  - MQ消息积压 > 10000 → 告警 (消费不足)
```

---

## 九、项目亮点

### 9.1 技术亮点

```
1. 全自研基础组件
   ✅ 网络库 (Reactor + Epoll, 百万并发)
   ✅ RPC框架 (TCP + Protobuf, 服务治理)
   ✅ 消息队列 (共享内存 + 磁盘, 零拷贝)
   ✅ 异步日志 (双缓冲 + 零拷贝)
   ✅ 线程池 (工作窃取 + CPU亲和)
   ✅ 内存池 (对象复用 + 缓存对齐)

2. 分布式架构设计
   ✅ 微服务拆分 (认证/消息/社交)
   ✅ 服务注册发现 (Consul)
   ✅ 负载均衡 (Nginx + 一致性哈希)
   ✅ RPC + MQ混合通信
   ✅ 分布式Session管理 (JWT + Redis)

3. 高性能优化
   ✅ 多级缓存 (本地+Redis+MySQL)
   ✅ 推拉结合消息模型
   ✅ 分库分表 (16库×64表)
   ✅ 零拷贝技术
   ✅ 无锁数据结构

4. 高可用保障
   ✅ 熔断降级 (Hystrix风格)
   ✅ 限流防刷 (令牌桶)
   ✅ 优雅停机 (K8s preStopHook)
   ✅ 全链路监控 (Prometheus+Jaeger)
   ✅ 故障自动恢复 (Consul健康检查)
```

### 9.2 解决的问题

```
难题1: 高并发下的消息风暴
  方案: MQ削峰填谷 + 限流降级
  效果: 系统稳定, 无雪崩

难题2: 万人大群消息扩散
  方案: 推拉结合模型 (小群推/大群拉)
  效果: 存储节省80%

难题3: 分布式Session管理
  方案: JWT + Redis共享存储
  效果: 水平扩展无感知

难题4: 消息可靠不丢不重
  方案: 本地消息表 + 去重机制 + 补偿任务
  效果: 消息可靠送达率99.999%

难题5: 海量历史消息检索
  方案: 分库分表 + ES索引 + 冷热分离
  效果: 查询延迟<100ms
```

---

## 十、与其他方案对比

| 维度 | **本方案 (C++自研)** | **OpenIM (Go)** | **商业IM (腾讯云)** |
|------|---------------------|-----------------|-------------------|
| **技术自主** | ✅ 完全自研 | ⚠️ 开源依赖 | ❌ 黑盒 |
| **性能** | ✅ 极致优化 | 中等 | ✅ 商业优化 |
| **成本** | 开发成本高 | 部署成本低 | 付费使用 |
| **定制化** | ✅ 完全定制 | ⚠️ 有限定制 | ❌ 无定制 |
| **学习价值** | ✅ 极高 | 中等 | 无 |
| **可控性** | ✅ 完全可控 | 中等 | 不可控 |
| **维护** | 需要团队 | 社区支持 | 厂商支持 |

---

## 🎯 总结

这套**C++自研分布式IM系统**是一个**从零构建、完全自主可控**的完整项目：

```
核心技术栈:
  C++17 + 自研网络库 + 自研RPC + 自研MQ + MySQL + Redis + ES

架构特点:
  微服务 + 分库分表 + 多级缓存 + 推拉结合 + 高可用保障

性能指标:
  10万日活 + 10000 TPS + P99<100ms + 99.99%可用性

项目价值:
  ✅ 简历含金量: 冲击大厂SP/SSP offer
  ✅ 技术深度: 自研全部基础组件
  ✅ 工程能力: 完整的分布式系统落地
  ✅ 问题解决: 高并发/高可用/高性能
```